# Git provenance

- `original_repository_history = unavailable`
- `reconstructed_history = false`
- `new_history_origin = POST_AUDIT_BASELINE`

The supplied ZIP contained no `.git` directory. The surrounding workspace repository also had no
valid `HEAD`. Any commit created in this checkout is a new post-audit history and is not evidence of
the original development history or the commit asserted by `delivery/STATUS.json`.
