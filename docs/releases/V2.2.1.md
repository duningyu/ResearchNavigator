# V2.2.1 — Verification and compatibility patch

This patch contains no new product capability. It fixes strict TypeScript nullability in Direction Map, corrects test selectors/router setup, preserves delivery metadata schema, and records the current evidence-bound verification status. Remaining external gates are explicitly PARTIAL/BLOCKED.
